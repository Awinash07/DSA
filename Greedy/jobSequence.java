/**
 * Job sequencing problem
 *
 * Given an array of jobs where every job has a deadline and profit if the job is finished before the deadline.
 * it is also given that every job takes a single job takes a single unit of time ,
 * so the minimum possible deadline for any job is 1. Maximum the total profit if only one job can be schedule at time.
 *
 *
 */
package Greedy;

public class jobSequence {
}

